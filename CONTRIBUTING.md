# Contributing

Open an issue before changing the scientific scope, joined dataset, threshold or inference. Submit
focused changes with tests and documentation; run lint, format, typing, tests, package build,
registry comparison and the full site suite. Never rewrite the frozen v0.1 registry without a new
versioned protocol and review. Preserve provenance, licensing, uncertainty and epistemic limits.
