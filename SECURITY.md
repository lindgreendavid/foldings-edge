# Security policy

Security fixes target the latest release. Report vulnerabilities with GitHub private vulnerability
reporting, not a public issue. The deployed laboratory accepts no uploads or authentication and
processes bounded controls client-side. CI audits production dependencies; upstream build-only
advisories and their deployment boundary are documented by the security audit rather than hidden.
